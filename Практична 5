#include <stdio.h> 
 
int main() { 
  // Оголошення змінних 
  int n; 
  long long count = 0; 
 
  // Введення n 
  printf("Введіть довжину послідовності n: "); 
  scanf("%d", &n); 
 
  // Перевірка введеного n 
  if (n < 1 || n > 10000) { 
    printf("Невірне число n. Число n має бути в діапазоні від 1 до 10000.\n"); 
    return 1; 
  } 
 
  // Обчислення кількості послідовностей 
  for (int i = 0; i <= n; i++) { 
    // Обчислення кількості послідовностей довжиною i 
    long long prev = 1;  // Кількість послідовностей довжиною i - 1 
    long long curr = 1;  // Кількість послідовностей довжиною i 
    for (int j = 2; j <= i; j++) { 
      if (j == 2) { 
        curr = 2; 
      } else { 
        curr = prev + curr; 
      } 
      prev = curr; 
    } 
 
    // Додавання кількості послідовностей довжиною i до загальної кількості 
    count = (count + curr) % 12345; 
  } 
 
  // Виведення кількості шуканих послідовностей 
  printf("Кількість шуканих послідовностей: %lld\n", count); 
 
  return 0; 
}
